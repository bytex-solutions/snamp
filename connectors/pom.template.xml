<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.bytex.snamp</groupId>
        <artifactId>snamp</artifactId>
        <version>2.0.0</version>
    </parent>
    <artifactId>connectors</artifactId>
    <packaging>pom</packaging>
    <name>SNAMP Connectors</name>
    <version>${snamp.connectors.version}</version>
    <description>Pack of SNAMP standard connectors</description>
    <build>
        <resources>
            <resource>
                <filtering>true</filtering>
                <directory>src/main/resources</directory>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.karaf.tooling</groupId>
                <artifactId>karaf-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>add-features-to-repo</id>
                        <goals>
                            <goal>features-generate-descriptor</goal>
                        </goals>
                        <configuration>
                            <inputFile>src/main/resources/features.xml</inputFile>
                        </configuration>
                    </execution>
                    <execution>
                        <goals>
                            <goal>features-create-kar</goal>
                        </goals>
                        <phase>package</phase>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <modules>
        <module>jmx-connector</module>
        <module>snmp-connector</module>
        <module>rshell-connector</module>
        <module>composite-connector</module>
        <module>groovy-connector</module>
        <module>modbus-connector</module>
        <module>message-driven-connector</module>
        <module>http-acceptor</module>
        <module>zipkin-connector</module>
    </modules>
</project>
