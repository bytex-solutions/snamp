<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.2.0" name="snamp">
    <feature name="${project.artifactId}" description="${project.name}" version="${project.version}">
        <details>${project.description}</details>
        <feature version="${snamp.framework.version}">platform-feature</feature>
        <bundle>mvn:com.itworks.snamp/snmp4j/${snmp4j.version}</bundle>
        <bundle>mvn:com.itworks.snamp/snmp-connector/${project.version}</bundle>
    </feature>
</features>