<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.2.0" name="snamp">
    <repository>mvn:org.apache.activemq/activemq-karaf/${activemq.version}/xml/features-core</repository>
    <feature name="${project.artifactId}" description="${project.name}" version="${project.version}">
        <details>${project.description}</details>
        <feature version="${activemq.version}">activemq-client</feature>
        <bundle start="true">mvn:org.codehaus.groovy/groovy/${groovy.version}/jar/indy</bundle>
        <bundle start="true">mvn:org.apache.ivy/ivy/${apache.ivy.version}</bundle>
        <bundle start="true">mvn:com.hazelcast/hazelcast/${hazelcast.version}</bundle>
        <bundle start="true">mvn:com.bytex.snamp/mq-connector/${project.version}</bundle>
    </feature>
</features>