<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.2.0" name="snamp-web-console">
    <repository>mvn:org.apache.karaf.features/standard/${karaf.version}/xml/features</repository>
    <feature name="${project.artifactId}" description="${project.name}" version="${project.version}">
        <details>${project.description}</details>
        <feature version="[${snamp.version}, ${snamp.maxVersion})">snamp-core</feature>
        <feature version="[${snamp.version}, ${snamp.maxVersion})">snamp-web-support</feature>
        <feature version="[${snamp.version}, ${snamp.maxVersion})">snamp-security</feature>
        <feature version="[${snamp.version}, ${snamp.maxVersion})">snamp-management</feature>
        <bundle start="true">mvn:com.bytex.snamp/webconsole/${snamp.webconsole.version}</bundle>
    </feature>
</features>
