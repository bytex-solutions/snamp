<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.2.0" name="snamp">
    <feature name="${project.artifactId}" description="${project.name}" version="${project.version}">
        <details>${project.description}</details>
        <feature version="[${snamp.framework.version}, ${snamp.framework.maxVersion})">snamp-core</feature>
        <feature version="[${snamp.framework.version}, ${snamp.framework.maxVersion})">snamp-jmx-support</feature>
        <feature version="[${snamp.framework.version}, ${snamp.framework.maxVersion})">snamp-ssh-support</feature>
        <bundle start="true">mvn:com.bytex.snamp/ssh-gateway/${project.version}</bundle>
    </feature>
</features>